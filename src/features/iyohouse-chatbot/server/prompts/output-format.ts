export const OUTPUT_FORMAT_INSTRUCTION = `Output format:
- Start with the direct answer.
- Keep the answer concise, but include enough context to be useful.
- End with a short source line when wiki pages support the answer.
- If the wiki does not contain enough public information, say so plainly instead of guessing.`;
